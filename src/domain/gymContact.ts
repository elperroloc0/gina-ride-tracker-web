/**
 * The gym's real contact info - used wherever the app needs to show or link
 * to it. Previously duplicated as a raw string in Login.tsx and
 * LoginDesktop.tsx; now the one place to update it. Real values, not
 * samples - phone matches design/DESIGN-SYSTEM.md's own copy, email
 * confirmed directly with the gym.
 */
export const GYM_PHONE_E164 = '+13054564150';
export const GYM_PHONE_DISPLAY = '1 (305) 456-4150';
export const GYM_EMAIL = 'hello@ginasgymnastics.com';
