---
category: Console
---

# TableHeader

The column header strip: 44px tall, `--surface-2`, 11/700 uppercase `--muted`
with 0.08em tracking. Takes the same `TableCell` children as a row, so the
columns line up by construction.

See `Table` for the full example.
